function letter  = checkChar(letter, img, alphabet)
%These tend to have a lot of mix ups
if letter == 'V' || letter == 'X' || letter == 'J' || letter == 'N' || letter == 'L' || letter == '0'
    letter = 
end
%These tend to overlap
if letter == '1' || letter == '7'
    letter = checkVertical(img);
end
if letter == 'V' || letter == 'J' || letter == 'N'
    
end